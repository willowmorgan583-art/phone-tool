pirate101@DEBIAN13:~$ cat > /tmp/pifix.sh << 'EOF'
#!/bin/bash
DBPATH=/home/pirate101/.local/share/waydroid/data/adb/magisk.db

echo "===== 1/7 Stop everything ====="
waydroid session stop 2>/dev/null
sudo systemctl stop waydroid-container; sleep 5
pkill -9 -f waydroid 2>/dev/null; sleep 2

echo ""
echo "===== 2/7 Enable Magisk DenyList (so PIF can target GMS) ====="
sudo sqlite3 $DBPATH "UPDATE settings SET value=1 WHERE key='denylist';"
sudo sqlite3 $DBPATH "SELECT * FROM settings;" | sed 's/^/  /'

echo ""
echo "===== 3/7 Pre-populate denylist hidelist (GMS family) ====="
# Add these BEFORE container boot so PIF's post-fs-data finds them already set
sudo sqlite3 $DBPATH "DELETE FROM hidelist;"
for entry in \
  "com.google.android.gms|com.google.android.gms.unstable" \
  "com.google.android.gms|com.google.android.gms" \
  "com.google.android.gms|com.google.android.gms.persistent" \
  "com.android.vending|com.android.vending" \
  "com.google.android.gsf|com.google.android.gsf"; do
  pkg="${entry%|*}"; proc="${entry#*|}"
  sudo sqlite3 $DBPATH "INSERT INTO hidelist (package_name, process) VALUES ('$pkg', '$proc');"
done
echo "  hidelist now:"
sudo sqlite3 $DBPATH "SELECT * FROM hidelist;" | sed 's/^/    /'

echo ""
echo "===== 4/7 Cold start ====="
bash /tmp/pifix.shoid shell -- cat /data/adb/pif.log"e Waydroid UI."ist ====="
===== 1/7 Stop everything =====

===== 2/7 Enable Magisk DenyList (so PIF can target GMS) =====
  zygisk|1
  denylist|1
  hide|1
  sulist|1

===== 3/7 Pre-populate denylist hidelist (GMS family) =====
  hidelist now:
    com.google.android.gms|com.google.android.gms.unstable
    com.google.android.gms|com.google.android.gms
    com.google.android.gms|com.google.android.gms.persistent
    com.android.vending|com.android.vending
    com.google.android.gsf|com.google.android.gsf

===== 4/7 Cold start =====

===== 5/7 Verify denylist is now ENFORCING =====
  denylist status:  

  hidden processes:

  zygisk loaded modules:
    fake_proc
    playintegrityfix
    zygisk_lsposed

  PIF module visible from GMS:
    total 52
    drwxr-xr-x 2 root   root  4096 2026-04-25 23:36 .
    drwxr-xr-x 5 system radio 4096 2026-04-25 19:18 ..
    -rw-r--r-- 1 root   root   615 2026-04-23 15:29 action.sh
    -rwxr-xr-x 1 root   root  5304 2026-04-23 15:29 autopif.sh

===== 6/7 Force-stop GMS + Vending so they re-init under denylist =====
Success
  cleared. waiting 10s for them to relaunch...

===== 7/7 Trigger PI verdict + read PIF log =====
  Run Play Integrity API Checker NOW from inside Waydroid UI.
  After it finishes, run this to see PIF log:

  sudo waydroid shell -- cat /data/adb/pif.log
[1]+  Done                    nohup waydroid session start > /tmp/sess_C.log 2>&1
pirate101@DEBIAN13:~$ 
